select experiment, schedule, datasize, count(*), avg(value) D, max(value), min(value)
from lwmon_data
where host like 'client%'
 and name = 'experiment'
 and key = 'wallclock'
group by experiment, schedule, datasize
order by schedule, datasize, D;

